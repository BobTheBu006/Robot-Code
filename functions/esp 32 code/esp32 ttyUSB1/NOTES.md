# ESP32 Workspace Notes

- Port: `/dev/ttyUSB1`
- Display: `CP2102N USB to UART Bridge Controller`
- This folder stores the local source files and block blueprint metadata that the Pi UI edits.
- Use the Function Builder's Build + Flash action to compile and upload these sources to the board from the Pi.
