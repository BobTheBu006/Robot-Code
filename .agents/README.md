# Agent Notes

Use the root `AGENTS.md` file as the active agent guide.

This folder is reserved for future machine-readable agent state, checklists, or automation hooks. Do not put long-lived project status here unless `AGENTS.md` is updated to point to it.
